# apps/cms

Strapi instance that serves as Promocean's config plane — achievement
definitions, offers, timed events, and API key management — read by
`apps/api` at runtime.

See the root [README](../../README.md) for full setup, environment
variables, and how to boot the whole stack.
